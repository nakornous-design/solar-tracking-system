# Production Status - Latest

Last updated: 2026-05-17

## Current State

- Production URL: `https://solar-tracking-system.vercel.app`
- Latest production deployment: `dpl_2fXNFJd17VwYf2fL1ZanfuCbYZkU`
- Release prepared for deploy: `v0.2.0`
- Current production mode: live with real users
- Current tune-up mode: stabilize, verify, and avoid broad refactors

## Release v0.2.0 - Project Detail UI Upgrade

- Scope: reversible UI polish for Project Detail, workflow rail, summary cards, activity timeline, mini progress, sidebar tooltips, stage icons, and Dashboard KPI strip.
- Backend behavior: no workflow logic, gate validation, database migration, or production data mutation.
- Version files: `package.json` and `package-lock.json` bumped from `0.1.0` to `0.2.0`.
- Rollback reference before this upgrade: `rollback-2026-05-17-production-ready` (`3d8872b6c1fd9b6586e871d77e3c1745e66aa85b`).
- Release tag: `v0.2.0-project-detail-ui-upgrade`.

## Verified

- `npm.cmd run lint -- --quiet`: pass
- `npm.cmd run test`: pass, 94/94
- `npx.cmd tsc --noEmit`: pass
- `npm.cmd run build`: pass
- Frontend API references matched backend route files: no missing API routes.
- `/api/health`: `ok: true`
- `/api/health` checks: env `ok`, database `ok`, Drive `ok`
- Cleanup baseline: removed local dev/build log artifacts; `.next` is treated as ignored generated output and can be regenerated by `npm.cmd run build`.
- Final audit baseline: API routes require auth except health, bootstrap, and postal lookup; danger permissions are assigned only to `system_admin`.
- Active published workflow v4 has 18 stages, 20 transitions, no broken transition references, and starts at `LEAD` / ends at `CLOSURE`.
- Auth tune-up: logout clears in-memory project/user/role/schedule/document state and unauthenticated users see a sign-in prompt instead of stale operational data.
- Header now shows the signed-in user's role beside their email.
- Users/profiles: 15 total, 15 active
- Active `system_admin`: 1
- Danger-zone permissions: only `system_admin`
- Active projects at final audit: 0
- Open exceptions: 0
- Pending approvals: 0
- Over-SLA stages: 0

## Final Audit Items To Review

- Active workflow name is long: `RES-S Standard v1 Draft V2 Draft V3 Draft V4`; rename later if operators need cleaner display.
- Active workflow includes `ตัด_MAT`; confirm this is an intentional production stage because it is not in the original MVP source-of-truth flow.
- Both `V8R2` and `V9` installation standards are active; project creation defaults to `V8R2` unless a standard is selected, but ops should confirm the intended active default.
- Generic transition API currently supports FORWARD only; QA, billing, finance path, and loan fallback use dedicated endpoints. Add broader transition actions later if required.

## Known Remaining Blocker

Google Drive is currently working through OAuth fallback.

This is acceptable for short-term production continuity, but it is not the final durable state.

The remaining blocker is external to the codebase:

- A Google service account JSON key must be provided.
- The Drive parent folder must be shared with that service account email as Editor.
- Vercel Production must receive `GOOGLE_SERVICE_ACCOUNT_JSON`.

Done means `/api/health` returns:

- `driveCredentialMode: "service_account"`
- `warnings: []`

## Known Non-Blocking Debt

- `npm.cmd run lint` reports warnings for existing `any` usage and some React/use-unused warnings.
- These warnings are intentionally not release-blocking during production tune-up.
- Reduce warnings incrementally in small, tested slices.

## Production Rule

Do not claim Drive is permanently fixed while `/api/health` reports `driveCredentialMode: "oauth"`.
